Вырезано из https://www.coursera.org/learn/supervised-learning/quiz/nTTV1/probliema-pierieobuchieniia 

Поздравляем! Вы прошли тест! 

loading    


Load Error! 

Вопрос 1 

Правильно 

1 из 1 

Баллы 

1. Вопрос 1 

Переобучение — это явление, при котором полученный при обучении алгоритм... 

показывает сопоставимое качество на обучающей выборке и новых данных. 

показывает на новых данных более низкое качество, чем на обучающей выборке. 

Правильно  

Алгоритм переобучается на обучающей выборки, как следствие на ней работает хорошо, но обобщает плохо. 

показывает на обучающей выборке более низкое качество, чем на новых данных. 

Вопрос 2 

Правильно 

1 из 1 

Баллы 

2. Вопрос 2 

Что из перечисленного является методами борьбы с переобучением? 

Усложнение модели 

правильно, этот вариант не должен быть выбран  

Уменьшение объёма обучающей выборки 

правильно, этот вариант не должен быть выбран  

Регуляризация 

Правильно  

Добавление слагаемого, штрафующего за сложность модели, является часто используемым методом борьбы с переобучением. 

Вопрос 3 

Правильно 

1 из 1 

Баллы 

3. Вопрос 3 

Предположим, вы решаете задачу восстановления регрессии с использованием некоторого функционала ошибки Q(w,X)Q(w, X)Q(w,X). Как будет выглядеть новый функционал при добавлении L2L_{2}L2 -регуляризатора с коэффициентом регуляризации λ\lambdaλ? 

Q(w,X)+λ∑j=1dwj2Q(w, X) + \lambda \sum_{j=1}^d w_j^2Q(w,X)+λ∑j=1d wj2  

Правильно  

Добавочное слагаемое является квадратом L2L_{2}L2 нормы весов линейной модели. 

Q(w,X)+λ∑j=1dwjQ(w, X) + \lambda \sum_{j=1}^d w_jQ(w,X)+λ∑j=1d wj  

Q(w,X)+λ∑j=1d∣wj∣Q(w, X) + \lambda \sum_{j=1}^d |w_j|Q(w,X)+λ∑j=1d ∣wj ∣ 

Q(w,X)+λ∑j=1d1wj2Q(w, X) + \lambda \sum_{j=1}^d \frac{1}{w_j^2}Q(w,X)+λ∑j=1d wj2 1  
